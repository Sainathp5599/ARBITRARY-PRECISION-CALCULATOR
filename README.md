# ARBITRARY-PRECISION-CALCULATOR
This project implements arithmetic operations on very large integers  using a doubly linked list (Dlist) in C.  Each digit of a number is stored in a node, allowing operations on numbers   far beyond the limits of standard data types.   
